spring:
  application:
    name: camel-dwarf-racing-api
  datasource:
    url: jdbc:postgresql://localhost:5432/camel_dwarf_racing
    username: race_admin
    password: race_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
team:
  max-members: 5
jwt:
  secret: "change-this-to-a-long-random-string-at-least-256-bits-for-hs256"
  expiration-ms: 3600000
